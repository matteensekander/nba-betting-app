PORT=3001
# Optional: Get a free API key at https://www.balldontlie.io to unlock the v1 API
# Without a key the app uses the older free endpoint (covers seasons through ~2023-24)
BALLDONTLIE_API_KEY=
FRONTEND_URL=http://localhost:5173
